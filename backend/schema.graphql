directive @oneOf on INPUT_OBJECT

directive @requireRoles repeatable on FIELD_DEFINITION

type AccessSummary {
  activeUsersCount: Int!
  deactivatedUsersCount: Int!
  ssoUsersCount: Int!
  successfulSignIns24h: Int!
  signInFailures24h: Int!
}

union AdminResetMfaPayload = UserType | OperationInfo

union AdminResetPasswordPayload = UserType | OperationInfo

union AssignPermissionPayload = PermissionType | RoleError | OperationInfo

input AssignRoleInput {
  userId: ID!
  roleName: String!
}

union AssignRolePayload = UserType | RoleError | OperationInfo

type AuditEventType {
  id: String!
  eventType: String!
  actor: String!
  detail: String!
  createdAt: DateTime!
  ipAddress: String
}

type AuditSummary {
  openFindingsCount: Int!
  overdueCorrectiveActionsCount: Int!
}

type AuthError {
  message: String!
}

union BeginMfaSetupPayload = MfaSetupType | OperationInfo

union ChangePasswordPayload = UserType | OperationInfo

type ChatAttachmentType {
  id: ID!
  url: String!
  width: Int!
  height: Int!
}

type ChatCallResult {
  joinUrl: String!
}

type ChatContactType {
  id: ID!
  participant: ChatParticipantType!
  conversationId: ID
  unreadCount: Int!
  lastMessagePreview: String!
  lastMessageAt: DateTime
}

type ChatMessageType {
  id: ID!
  conversationId: ID!
  body: String!
  createdAt: DateTime!
  sender: ChatParticipantType!
  mine: Boolean!
  attachments: [ChatAttachmentType!]!
}

type ChatOpenResult {
  conversationId: ID!
  unreadTotal: Int!
}

type ChatParticipantType {
  id: ID!
  name: String!
  email: String!
  department: String!
  avatarUrl: String
  online: Boolean!
  lastSeenAt: DateTime
}

type ChatPresenceEvent {
  userId: ID!
  online: Boolean!
}

type ChatReadResult {
  conversationId: ID!
  unreadTotal: Int!
}

type ChatTypingEvent {
  userId: ID!
  typing: Boolean!
}

union ConfirmMfaSetupPayload = MfaConfirmedType | OperationInfo

union CreateRiskPayload = RiskType | OperationInfo

union CreateUserPayload = UserType | OperationInfo

"""Date (isoformat)"""
scalar Date

"""Date with time (isoformat)"""
scalar DateTime

union DeleteUserPayload = UserType | OperationInfo

union DisableMfaPayload = UserType | OperationInfo

"""permission | action"""
enum IamPermissionActionEnum {
  """View"""
  view

  """Create"""
  create

  """Edit"""
  edit

  """Delete"""
  delete

  """Approve"""
  approve

  """Assign"""
  assign
}

"""permission | resource"""
enum IamPermissionResourceEnum {
  """Users"""
  iam_users

  """Roles"""
  iam_roles

  """Risk register"""
  risk

  """Controls"""
  controls

  """Audit findings"""
  audit

  """Incidents"""
  incidents

  """Obligations"""
  obligations
}

"""role | name"""
enum IamRoleNameEnum {
  """Admin"""
  admin

  """CISO"""
  ciso

  """Risk Manager"""
  risk_manager

  """Auditor"""
  auditor

  """Control Owner"""
  control_owner

  """Viewer"""
  viewer
}

input IamRoleNameEnumFilterLookup {
  """Exact match. Filter will be skipped on `null` value"""
  exact: IamRoleNameEnum

  """Assignment test. Filter will be skipped on `null` value"""
  isNull: Boolean

  """
  Exact match of items in a given list. Filter will be skipped on `null` value
  """
  inList: [IamRoleNameEnum!]

  """Case-insensitive exact match. Filter will be skipped on `null` value"""
  iExact: IamRoleNameEnum

  """
  Case-sensitive containment test. Filter will be skipped on `null` value
  """
  contains: IamRoleNameEnum

  """
  Case-insensitive containment test. Filter will be skipped on `null` value
  """
  iContains: IamRoleNameEnum

  """Case-sensitive starts-with. Filter will be skipped on `null` value"""
  startsWith: IamRoleNameEnum

  """Case-insensitive starts-with. Filter will be skipped on `null` value"""
  iStartsWith: IamRoleNameEnum

  """Case-sensitive ends-with. Filter will be skipped on `null` value"""
  endsWith: IamRoleNameEnum

  """Case-insensitive ends-with. Filter will be skipped on `null` value"""
  iEndsWith: IamRoleNameEnum

  """
  Case-sensitive regular expression match. Filter will be skipped on `null` value
  """
  regex: IamRoleNameEnum

  """
  Case-insensitive regular expression match. Filter will be skipped on `null` value
  """
  iRegex: IamRoleNameEnum
}

"""user | auth provider"""
enum IamUserAuthProviderEnum {
  """Local"""
  local

  """Microsoft Entra ID"""
  entra_id
}

type IncidentSummary {
  openCount: Int!
}

union LoginResult = UserType | AuthError | MfaRequired

type MfaConfirmedType {
  user: UserType!
  recoveryCodes: [String!]!
}

type MfaRecoveryCodesType {
  recoveryCodes: [String!]!
}

type MfaRequired {
  message: String!
}

type MfaSetupType {
  secret: String!
  provisioningUri: String!
}

type Mutation {
  login(email: String!, password: String!): LoginResult!
  verifyMfaCode(code: String!): LoginResult!
  logout: Boolean!
  requestPasswordReset(email: String!): Boolean!
  resetPassword(uid: String!, token: String!, newPassword: String!): ResetPasswordPayload!
  changePassword(oldPassword: String!, newPassword: String!): ChangePasswordPayload!
  beginMfaSetup: BeginMfaSetupPayload!
  confirmMfaSetup(code: String!): ConfirmMfaSetupPayload!
  disableMfa(password: String!): DisableMfaPayload!
  regenerateMfaRecoveryCodes(password: String!): RegenerateMfaRecoveryCodesPayload!
  updateMyProfile(data: MyProfileInput!): UpdateMyProfilePayload!
  updateMyAvatar(imageBase64: String!): UpdateMyAvatarPayload!
  removeMyAvatar: RemoveMyAvatarPayload!
  adminResetMfa(userId: ID!): AdminResetMfaPayload! @requireRoles
  adminResetPassword(userId: ID!): AdminResetPasswordPayload! @requireRoles
  setMfaRequired(userId: ID!, required: Boolean!): SetMfaRequiredPayload! @requireRoles
  createUser(data: UserCreateInput!): CreateUserPayload! @requireRoles
  updateUser(userId: ID!, data: UserUpdateInput!): UpdateUserPayload! @requireRoles
  setUserActive(userId: ID!, isActive: Boolean!): SetUserActivePayload! @requireRoles
  deleteUser(userId: ID!): DeleteUserPayload! @requireRoles
  assignRole(data: AssignRoleInput!): AssignRolePayload! @requireRoles
  startAccessReview(userIds: [ID!]!): [UserType!]! @requireRoles
  revokeRole(data: AssignRoleInput!): RevokeRolePayload! @requireRoles
  assignPermission(data: RolePermissionInput!): AssignPermissionPayload! @requireRoles
  revokePermission(data: RolePermissionInput!): RevokePermissionPayload! @requireRoles
  createRisk(data: RiskCreateInput!): CreateRiskPayload! @requireRoles
  clearNotification(key: String!): [NotificationType!]!
  clearAllNotifications: [NotificationType!]!
  sendChatMessage(body: String = null, conversationId: ID = null, recipientId: ID = null, photos: [String!] = null): ChatMessageType!
  openChatConversation(recipientId: ID!): ChatOpenResult!
  markChatRead(conversationId: ID!): ChatReadResult!
  setChatTyping(conversationId: ID!, typing: Boolean!): Boolean!
  startChatCall(recipientId: ID!, video: Boolean!): ChatCallResult!
}

input MyProfileInput {
  firstName: String! = ""
  lastName: String! = ""
  department: String! = ""
}

type NotificationType {
  key: String!
  tone: String!
  title: String!
  detail: String!
  href: String!
}

type ObligationSummary {
  registeredCount: Int!
  reviewsDueSoonCount: Int!
}

input OffsetPaginationInput {
  offset: Int! = 0
  limit: Int
}

type OperationInfo {
  """List of messages returned by the operation."""
  messages: [OperationMessage!]!
}

type OperationMessage {
  """The kind of this message."""
  kind: OperationMessageKind!

  """The error message."""
  message: String!

  """
  The field that caused the error, or `null` if it isn't associated with any particular field.
  """
  field: String

  """The error code, or `null` if no error code was set."""
  code: String
}

enum OperationMessageKind {
  INFO
  WARNING
  ERROR
  PERMISSION
  VALIDATION
}

enum Ordering {
  ASC
  ASC_NULLS_FIRST
  ASC_NULLS_LAST
  DESC
  DESC_NULLS_FIRST
  DESC_NULLS_LAST
}

input PermissionOrder @oneOf {
  resource: Ordering
  action: Ordering
}

"""
Resource/action grants held by roles — the access-control catalog (A.5.15).
"""
type PermissionType {
  roles: [RoleType!]!
  id: ID!
  resource: IamPermissionResourceEnum!
  action: IamPermissionActionEnum!
  isoClause: String!
}

type Query {
  users(filters: UserFilter, order: UserOrder, pagination: OffsetPaginationInput): [UserType!]!
  roles(filters: RoleFilter, order: RoleOrder): [RoleType!]!
  permissions(order: PermissionOrder): [PermissionType!]!
  me: UserType
  usernameAvailable(username: String!): Boolean! @requireRoles
  auditEvents(limit: Int! = 50): [AuditEventType!]!
  myAuditEvents(limit: Int! = 25): [AuditEventType!]!
  accessSummary: AccessSummary!
  risks(order: RiskOrder): [RiskType!]!
  riskSummary: RiskSummary!
  soaSummary: SoaSummary!
  auditSummary: AuditSummary!
  incidentSummary: IncidentSummary!
  obligationSummary: ObligationSummary!
  notifications: [NotificationType!]!
  chatContacts(search: String = null, limit: Int = null): [ChatContactType!]!
  chatMessages(conversationId: ID!, limit: Int = null): [ChatMessageType!]!
  chatUnreadTotal: Int!
  globalSearch(query: String!, limit: Int! = 5): [SearchResult!]!
}

union RegenerateMfaRecoveryCodesPayload = MfaRecoveryCodesType | OperationInfo

union RemoveMyAvatarPayload = UserType | OperationInfo

union ResetPasswordPayload = UserType | OperationInfo

union RevokePermissionPayload = PermissionType | RoleError | OperationInfo

union RevokeRolePayload = UserType | RoleError | OperationInfo

input RiskCreateInput {
  title: String!
  inherentLikelihood: Int!
  inherentImpact: Int!
  description: String! = ""
  source: String! = ""
  event: String! = ""
  consequence: String! = ""
  ownerId: ID = null
  nextReviewDate: Date = null
}

type RiskLevelCount {
  level: String!
  count: Int!
}

input RiskOrder @oneOf {
  reference: Ordering
  title: Ordering
  status: Ordering
  nextReviewDate: Ordering
}

"""risk | status"""
enum RiskRiskStatusEnum {
  """Identified"""
  identified

  """Assessed"""
  assessed

  """Treatment Planned"""
  treatment_planned

  """Treated"""
  treated

  """Accepted"""
  accepted

  """Closed"""
  closed
}

type RiskSummary {
  openCount: Int!
  overdueForReviewCount: Int!
  profile: [RiskLevelCount!]!
}

"""
Risk(id, reference, title, description, source, event, consequence, status, inherent_likelihood, inherent_impact, residual_likelihood, residual_impact, next_review_date, owner, created_at)
"""
type RiskType {
  owner: UserType
  id: ID!
  reference: String!
  title: String!
  description: String!
  source: String!
  event: String!
  consequence: String!
  status: RiskRiskStatusEnum!
  inherentLikelihood: Int!
  inherentImpact: Int!
  residualLikelihood: Int
  residualImpact: Int
  nextReviewDate: Date
  inherentLevel: String!
  residualLevel: String
}

type RoleError {
  message: String!
}

"""Role(id, name)"""
input RoleFilter {
  name: IamRoleNameEnumFilterLookup
  AND: RoleFilter
  OR: RoleFilter
  NOT: RoleFilter
  DISTINCT: Boolean
}

input RoleOrder @oneOf {
  name: Ordering
}

input RolePermissionInput {
  roleName: String!
  permissionId: ID!
}

"""Role(id, name)"""
type RoleType {
  id: ID!
  name: IamRoleNameEnum!
}

type SearchResult {
  id: String!
  kind: String!
  label: String!
  sublabel: String!
  url: String!
}

union SetMfaRequiredPayload = UserType | OperationInfo

union SetUserActivePayload = UserType | OperationInfo

type SoaSummary {
  controlsInScope: Int!
  implementedPercentage: Float!
}

input StrFilterLookup {
  """Exact match. Filter will be skipped on `null` value"""
  exact: String

  """Assignment test. Filter will be skipped on `null` value"""
  isNull: Boolean

  """
  Exact match of items in a given list. Filter will be skipped on `null` value
  """
  inList: [String!]

  """Case-insensitive exact match. Filter will be skipped on `null` value"""
  iExact: String

  """
  Case-sensitive containment test. Filter will be skipped on `null` value
  """
  contains: String

  """
  Case-insensitive containment test. Filter will be skipped on `null` value
  """
  iContains: String

  """Case-sensitive starts-with. Filter will be skipped on `null` value"""
  startsWith: String

  """Case-insensitive starts-with. Filter will be skipped on `null` value"""
  iStartsWith: String

  """Case-sensitive ends-with. Filter will be skipped on `null` value"""
  endsWith: String

  """Case-insensitive ends-with. Filter will be skipped on `null` value"""
  iEndsWith: String

  """
  Case-sensitive regular expression match. Filter will be skipped on `null` value
  """
  regex: String

  """
  Case-insensitive regular expression match. Filter will be skipped on `null` value
  """
  iRegex: String
}

type Subscription {
  chatMessageReceived(conversationId: ID!): ChatMessageType!
  chatContactUpdated: ChatContactType!
  chatTypingChanged(conversationId: ID!): ChatTypingEvent!
  chatPresenceChanged: ChatPresenceEvent!
  sessionInvalidated: Boolean!
}

union UpdateMyAvatarPayload = UserType | OperationInfo

union UpdateMyProfilePayload = UserType | OperationInfo

union UpdateUserPayload = UserType | OperationInfo

input UserCreateInput {
  email: String!
  username: String!
  password: String!
  roleName: String!
  firstName: String! = ""
  lastName: String! = ""
  department: String! = ""
  requireMfa: Boolean! = false
  sendWelcomeEmail: Boolean! = false
}

"""Application user. Email is the login identifier."""
input UserFilter {
  email: StrFilterLookup
  lastName: StrFilterLookup
  AND: UserFilter
  OR: UserFilter
  NOT: UserFilter
  DISTINCT: Boolean
}

input UserOrder @oneOf {
  email: Ordering
  lastName: Ordering
}

"""Application user. Email is the login identifier."""
type UserType {
  roles: [RoleType!]!
  id: ID!
  lastLogin: DateTime

  """
  Designates that this user has all permissions without explicitly assigning them.
  """
  isSuperuser: Boolean!

  """Required. 150 characters or fewer. Letters, digits and @/./+/-/_ only."""
  username: String!
  firstName: String!
  lastName: String!

  """
  Designates whether this user should be treated as active. Unselect this instead of deleting accounts.
  """
  isActive: Boolean!
  dateJoined: DateTime!
  email: String!
  authProvider: IamUserAuthProviderEnum!
  entraObjectId: String
  department: String!
  mfaEnabled: Boolean!
  mfaRequired: Boolean!
  nextAccessReviewDate: Date
  mustChangePassword: Boolean!
  mfaRecoveryCodesRemaining: Int!
  avatarUrl: String
}

input UserUpdateInput {
  email: String!
  username: String!
  firstName: String! = ""
  lastName: String! = ""
  department: String! = ""
}